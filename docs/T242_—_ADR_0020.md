# T242 — ADR 0020

## Goal

Write `docs/adr/0020-program-identity-and-score-rubric.md`: why the **Program** has a first-class URL and why the **Program Score Rubric** is a published product claim (draft-to-defend, not mute gauges). Addresses the Tech Plan ADR row; no extra user story.

## Mode

AFK — decisions are already locked in CONTEXT + grilling. This ticket records them.

## Slice

ADR markdown only (follows existing `docs/adr/00xx-*.md` shape)

## Dependencies

T238 (rubric numbers exist in `bands.ts` so the ADR can point at them). Can run in parallel with T239–T241.

## Scope

### File

`file:docs/adr/0020-program-identity-and-score-rubric.md`

Status: Accepted. Date: implementation date.

### Must record

- **Program Page** is `/programs/:id`, sibling of `/builder/:id`, not `/library/programs/:id` and not a sheet
- Scores are **intent** (week as written), never `set_logs`
- Client-side `scoreProgram`; no `programs` score columns; no RPC
- Three **Goal Tracks** + **Program Balance** (0–100 only). No pin on `profile.goal`
- **Circuit in Program Scores**: first-class; AMRAP = Tours; presence once per station
- Bands live in `file:src/lib/programScore/bands.ts` — draft-to-defend, published
- Empty ≠ `short`
- Live Builder banner is **#519**, not this epic
- UI copy must not leak glossary (see T241)

Follow the tone of `file:docs/adr/0018-circuit-catalog-encyclopedia-under-library.md` (context / decision / consequences), not a second Tech Plan.

## Out of Scope

- Changing scorer or UI
- Rewriting the Epic Brief

## Acceptance Criteria

- [ ] ADR 0020 exists and states the bullets above
- [ ] It links the Epic Brief, Tech Plan, `bands.ts`, and #519
- [ ] No new product decision — if you want one, stop and grill

## References

- Epic Brief + Tech Plan #504
- Glossary terms listed in the Tech Plan header
