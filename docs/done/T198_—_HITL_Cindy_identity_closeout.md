# T198 — HITL Cindy identity closeout

## Goal

Human pass on the tracer: MCP/QW obtain Cindy by catalog id, run the shipped AMRAP **Round Screen**, history/PR under Cindy with real story copy, fork confirm feels right, `/library` and home jobs unchanged. Stories 3, 25 + epic success criteria.

## Mode

**HITL** — needs a human on device and an agent session. Copy/story was locked in the brief ; this is seeing it in the sheet, not rewriting it unless it clangs.

## Slice

HITL checklist → notes on the PR (no new feature work unless a bug from T191–T197)

## Dependencies

T192, T194, T195, T196, T197 (and transitively T191, T193).

## Scope

### Checklist

- MCP: `benchmark_slug: cindy` dry_run → apply → details echo slug ; second day → same PR in PWA sheet and `get_workout_history`.
- QW: constraints/intent Cindy or Holland → not a snowflake ; HIIT 20 min still jetable.
- Session: GO → TIME leftover → sheet shows story + glossed `27+3` ; Holland is copy, not a run.
- Builder: change cap → confirm fork → seed intact ; Monday run still cindy if you forked after a GO (T193).
- Negative: home has no new **Do Cindy** CTA ; `/library` not a WOD shelf ; no Stitch-only screens.

## Out of Scope

- Implementing #393. Writing new seed copy (unless HITL finds a typo). Backfill. Zeus.

## Acceptance Criteria

- [ ] HITL notes attached (comment or PR body) covering MCP, QW, sheet, fork, IA negatives.
- [ ] 0 Cindy persist on closed-intent without `benchmark_circuit_id`.
- [ ] No product-surface regression on home / library beyond catalog-linked history.

## References

- Epic Brief Success Criteria
- Tech Plan Failure Mode table
- T190 AMRAP closeout as format reference `file:docs/T190_—_HITL_AMRAP_closeout.md`
