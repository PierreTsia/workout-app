---
import Callout from './Callout.astro'

interface Props {
  title?: string
}

const {
  title = 'One-click install via Anthropic Directory — Coming soon',
} = Astro.props
---

<Callout type="tip" title={title}>
  <slot />
</Callout>
