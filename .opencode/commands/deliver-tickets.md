---
description: "Orchestrates AFK ticket delivery from the pinged thread: one subagent per AFK ticket, TDD for all code, one commit per AFK ticket, HITL surfaced as a concise\u2026"
---

Load the `deliver-tickets` skill with the skill tool, then follow it exactly.

User input (may be empty): $ARGUMENTS
