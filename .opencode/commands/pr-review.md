---
description: "Fetch and address GitHub PR review comments tagged with an \"eyes\" reaction on the current branch's pull request. Applies fixes, runs quality checks, commits\u2026"
---

Load the `pr-review` skill with the skill tool, then follow it exactly.

User input (may be empty): $ARGUMENTS
