---
description: "Create a new branch from a GitHub issue. Checks out main, pulls latest, and creates a branch following the convention type/issue-number/description. Fetches\u2026"
---

Load the `start-branch` skill with the skill tool, then follow it exactly.

User input (may be empty): $ARGUMENTS
